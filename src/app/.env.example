# Public environment variables (safe to commit - no secrets)
# Frontend-only placeholders for the full-site UI feature.
NEXT_PUBLIC_APP_NAME="Talent Hub"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# NOTE: Never put secrets (Neon DB, JWT, API keys) in NEXT_PUBLIC_*.
# Backend integration lands in a later feature and will use its own environment.